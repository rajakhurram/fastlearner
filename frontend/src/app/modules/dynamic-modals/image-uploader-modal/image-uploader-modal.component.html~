<div class="modal-content">
  <h2 class="image-cropper">Upload Photo</h2>
  <image-cropper
    [imageChangedEvent]="imageChangedEvent"
    [maintainAspectRatio]="true"
    [aspectRatio]="imageAspectRatio"
    format="jpeg"
    [resizeToHeight]="1080"
    [resizeToWidth]="1920"
    (imageCropped)="imageCropped($event)"
    (imageLoaded)="imageLoaded($event)"
    (cropperReady)="cropperReady()"
    (loadImageFailed)="loadImageFailed()"
  ></image-cropper>
  <p *ngIf="showMessage" class="note">
    Please upload an image with dimensions of 1920x1080 pixels.
  </p>

  <div
    class="uploader mt-4 ml-4 mr-4"
    style="display: flex; align-items: stretch"
  >
    <span class="file-upload-span w-100">
      <input
        #fileInput
        id="image-upload"
        type="file"
        style="display: none"
        (change)="fileChangeEvent(fileInput.files)"
        accept="image/*"
      />
      <label for="image-upload" class="custom-file-upload">
        <span class="upload-button">{{
          showReplaceButton ? "Replace" : "Browse"
        }}</span>
        <span *ngIf="selectedFileName" class="file-name">{{
          selectedFileName
        }}</span>
      </label>
    </span>
  </div>

  <div class="btn-cropper">
    <button nz-button class="cancelBtn" (click)="closeModal()">Cancel</button>
    <button
      [disabled]="uploading"
      nz-button
      class="saveBtn"
      (click)="saveModal()"
      
    >
      Save
    </button>
  </div>
</div>

<div class="modal-loader-overlay" *ngIf="uploading">
  <nz-spin nzSize="large" nzTip=""></nz-spin>
</div>
